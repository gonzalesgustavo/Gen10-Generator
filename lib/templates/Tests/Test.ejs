import supertest from "supertest";
import {app} from "../../server";
import {connection} from '../../Databases/Mongoose.switch';
import { Mongoose } from "mongoose";

describe('<%= name %> Tests', () => {
  let request: supertest.SuperTest<supertest.Test>;
  beforeAll(() => {
    connection().then();
    request = supertest(app);
  });
  afterAll(async () => {
    connection().then((db:Mongoose) => {
      db.connection.close();
    })
  })
  it('should ', async () => {
    // code here
  });
})
