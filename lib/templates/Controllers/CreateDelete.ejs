import { Request, Response } from 'express';

export default class <%= fileName %>Controller {
  static index(req: Request, res: Response): void {
    try{
      res.end('home endpoint')
    } catch(error){
      res.status(500).end();
    }
  }
  static create(req: Request, res: Response): void {
    try{
      res.end('create endpoint')
    } catch(error){
      res.status(500).end();
    }
  }
  static remove(req: Request, res: Response): void {
    try{
      res.end('remove endpoint')
    } catch(error){
      res.status(500).end();
    }
  }
}
