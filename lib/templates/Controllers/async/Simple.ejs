import { Request, Response } from "express";

export default class <%= fileName %>Controller {
  static async index(req: Request, res: Response): Promise<void> {
    try{
      res.end('async home endpoint')
    } catch(error){
      res.status(500).end();
    }
  }
}
