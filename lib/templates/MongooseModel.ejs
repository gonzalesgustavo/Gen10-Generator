import mongoose, { Schema, Document } from 'mongoose';

export interface <%= name %> extends Document{
  // create interface for model
}
export default mongoose.model<%- brName.toString() %>('<%= name %>', new Schema({}));