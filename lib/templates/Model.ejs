import { Collection } from 'mongodb';
import { Model } from '../../core/types.core';

export type <%= name %>ModelType = {};

export interface <%= name %> {}

export default class <%= name %>Model extends Model {
  constructor(schema: <%= name %>ModelType, db: Collection) {
    super({ schema, db });
    this.schema = schema;
    this.database = db;
  }
}
